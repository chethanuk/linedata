import setuptools

setuptools.setup(
    name="line_level_data_collection",
    version="0.0.1",
    author="david",
    author_email="david@cdatai.com",
    packages=setuptools.find_packages(exclude=("tests",)),
    python_requires=">=3.7"
)
